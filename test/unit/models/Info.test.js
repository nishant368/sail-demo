"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Info');
const Fixture = require('../../fixtures/InfoFixture');

describe('models:Info', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
