"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/InfoController');

describe('controllers:InfoController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
